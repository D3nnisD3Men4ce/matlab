%Baliguat, Dennis Ivan
n = -10 : 1 : 10;
rand_elems = randi([-5, 14], 1, length(n));

%A
subplot(2, 1, 1);
stem(n, rand_elems);
title("Random discrete signal");
xlabel('n');
ylabel('Amplitude');

%B

